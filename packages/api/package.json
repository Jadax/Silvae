{
  "name": "@silvae/api",
  "version": "0.4.1",
  "private": true,
  "description": "OpenAPI spec + generated web client for the Silvae serverless API.",
  "scripts": {
    "build": "node scripts/generate.mjs",
    "dev": "node scripts/generate.mjs --watch",
    "typecheck": "node scripts/generate.mjs && tsc --noEmit",
    "lint": "node scripts/generate.mjs && tsc --noEmit",
    "test": "echo \"no tests yet\""
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "typescript": "^7.0.2"
  }
}
