package org.silvae.notifications

/**
 * Notifications (A4):
 * - WorkManager schedules the next care notification per plant at the computed
 *   time (schedule-correct), no server cron.
 * - FCM used only for share/community (S-5/S-6).
 */
object NotificationsPlaceholder
