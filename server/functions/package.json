{
  "name": "@silvae/server",
  "version": "0.2.0",
  "private": true,
  "description": "Vercel serverless functions: plant-id proxy, weather proxy, share/notify.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@silvae/core": "workspace:*",
    "firebase-admin": "^14.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
